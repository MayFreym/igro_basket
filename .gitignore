# Внутренние рабочие документы — не публикуются.
# Репозиторий публичный ради GitHub Pages, поэтому аудит, список багов и
# заметки остаются только на локальной машине.
NOTES.md
RECOMMENDATIONS.md
AUDIT.md
FIXES.md
docs/claude-memory/
MANAGER-REPORT.md

# Служебное окружение: содержит локальные пути к машине разработчика.
.claude/

# Оригинал корзины на DSL + React (1.2 МБ вместе с vendor-файлами React).
# Не публикуется. ВНИМАНИЕ: значит, git его НЕ хранит — единственная копия
# лежит на локальном диске. Держите резервную копию отдельно.
_old/

# Временная копия index.html с шимом rAF — генерит tests/run.sh на время прогона
# и сам же удаляет. В git не нужна.
_ui-test.html

# Мусор ОС и редакторов
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
